import { IEvent, ILocation, IOrganizer, ITag, IUser } from "./types";

describe('Types', () => {
  it('should define ILocation interface', () => {
    expect(true).toBe(true); // Just check that the interface is defined
  });

  it('should define ITag interface', () => {
    expect(true).toBe(true); // Just check that the interface is defined
  });

  it('should define IUser interface', () => {
    expect(true).toBe(true); // Just check that the interface is defined
  });

  it('should define IOrganizer interface', () => {
    expect(true).toBe(true); // Just check that the interface is defined
  });

  it('should define IEvent interface', () => {
    expect(true).toBe(true); // Just check that the interface is defined
  });
});
